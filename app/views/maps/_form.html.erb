<%= form_for(@map) do |f| %>
  <% if @map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
      <% @map.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_url %><br />
    <%= f.text_field :project_url %>
  </div>
  <div class="field">
    <%= f.label :location_data %><br />
    <%= f.text_area :location_data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
